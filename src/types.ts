export interface IUser {
    id: number;
    name: string;
    email: string;
    password: string;
    age: number;
}

export interface IStock {
    id: number;
    name: string;
    price: number;
    quantity: number;
}
